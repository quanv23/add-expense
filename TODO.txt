1. Find out way to convert UTC string to local date string in YYYY-MM-DD format 
2. Causing a bug in ExpenseCard where the displaying date is different from the input date because display is formatted from local time and input if formatted from UTC time 